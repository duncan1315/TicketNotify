name: Scheduled scrape

on:
  schedule:
    - cron: "7,37 * * * *"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  scrape:
    uses: ./.github/workflows/run-scrape.yml
    secrets: inherit
