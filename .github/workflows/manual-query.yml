name: Manual query

on:
  workflow_dispatch:
    inputs:
      origin:
        description: Origin airport code
        required: true
      destination:
        description: Destination airport code
        required: true
      date:
        description: Departure date (YYYY-MM-DD)
        required: true
      budget:
        description: Budget threshold
        required: false
        default: "999999"
  issue_comment:
    types: [created]

jobs:
  query:
    if: github.event_name == 'workflow_dispatch' || startsWith(github.event.comment.body, '/check')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r scraper/requirements.txt
          playwright install chromium --with-deps

      - name: Run manual query
        working-directory: scraper
        env:
          EVENT_NAME: ${{ github.event_name }}
          COMMENT_BODY: ${{ github.event.comment.body }}
          INPUT_ORIGIN: ${{ github.event.inputs.origin }}
          INPUT_DESTINATION: ${{ github.event.inputs.destination }}
          INPUT_DATE: ${{ github.event.inputs.date }}
          INPUT_BUDGET: ${{ github.event.inputs.budget }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python manual_query.py

      - name: Upload debug snapshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: manual-query-debug-${{ github.run_id }}
          path: scraper/debug
          if-no-files-found: ignore
          retention-days: 3

      - name: Reply on issue
        if: github.event_name == 'issue_comment'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const result = fs.readFileSync('scraper/manual_query_result.txt', 'utf8');
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: result,
            });
